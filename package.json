{
  "name": "typographix-v6",
  "version": "1.0.0",
  "description": "Typographix Blog using SCSS",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-dev": "sass --watch --source-map scss/main.scss:css/compiled_style.css",
    "sass-prod": "sass --no-source-map --style=compressed scss/main.scss:css/compiled_style.css "
  },
  "author": "Timothy Sanders",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.77.4"
  }
}
